#include<stdio.h>
#include<conio.h>

main() {
	int n, count = 0, rem;
	clrscr();

	printf("Write Any Number : ");
	scanf("%d",&n);

	while(n != 0) {
		count++;
		n = n / 10;
	}

	printf("%d",count);

	getch();
}